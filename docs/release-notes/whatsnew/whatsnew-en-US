### Bug Fixes

* **newsevents:** clear the load-more flag on every reload path
* **newsevents:** clear the load-more flag when a fetch is cancelled
* **newsevents:** dedupe paginated appends and close load-more race, closes #182 #179
